// CardBox.js
import React from 'react';
import './CardBox.css'; // Import the CSS file for styling

const CardBox = ({ children }) => {
  return <div className="card-box">{children}</div>;
};

// export default CardBox;
